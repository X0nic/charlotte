require 'spec_helper'

describe Charlotte do
  it 'has a version number' do
    expect(Charlotte::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
